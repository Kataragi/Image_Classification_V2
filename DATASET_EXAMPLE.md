# 📁 データセット構造例

このドキュメントでは、データセットの配置方法について説明します。

## 🎯 基本構造

```
dataset/
├── anime/           # アニメ塗り
├── brush/           # ブラシ塗り
├── thick/           # 厚塗り
├── watercolor/      # 水彩
├── photo/           # 写真
├── 3dcg/            # 3DCG
├── comic/           # 白黒マンガ
└── pixelart/        # ピクセルアート
```

## 📋 各クラスの説明

### 1. anime (アニメ塗り)
- アニメ調のデジタルイラスト
- セルシェーディング
- はっきりした色分け
- デジタルペイント

**配置例:**
```
dataset/anime/
├── anime_001.jpg
├── anime_002.png
├── character_01.jpg
└── ...
```

### 2. brush (ブラシ塗り)
- ブラシストロークが見える塗り
- デジタルブラシペイント
- 筆のタッチが残る塗り方

**配置例:**
```
dataset/brush/
├── brush_painting_001.jpg
├── digital_brush_002.png
└── ...
```

### 3. thick (厚塗り)
- 油絵のような厚みのある塗り
- レイヤーを重ねた塗り方
- リアルな質感

**配置例:**
```
dataset/thick/
├── oil_style_001.jpg
├── thick_paint_002.jpg
└── ...
```

### 4. watercolor (水彩)
- 水彩画風
- 透明感のある塗り
- にじみやぼかしの表現

**配置例:**
```
dataset/watercolor/
├── watercolor_001.jpg
├── aquarelle_002.png
└── ...
```

### 5. photo (写真)
- 実写写真
- フォトリアリスティック
- 実際の写真データ

**配置例:**
```
dataset/photo/
├── photo_001.jpg
├── realistic_002.jpg
└── ...
```

### 6. 3dcg (3DCG)
- 3Dレンダリング画像
- CGモデル
- 3Dソフトウェアで制作

**配置例:**
```
dataset/3dcg/
├── render_001.png
├── 3d_model_002.jpg
└── ...
```

### 7. comic (白黒マンガ)
- モノクロマンガ
- 白黒イラスト
- コミックスタイル

**配置例:**
```
dataset/comic/
├── manga_page_001.jpg
├── comic_002.png
└── ...
```

### 8. pixelart (ピクセルアート)
- ドット絵
- レトロゲーム風
- ピクセル単位で描かれた絵

**配置例:**
```
dataset/pixelart/
├── pixel_001.png
├── retro_game_002.png
└── ...
```

## ⚙️ 推奨設定

### ファイル形式
- **推奨**: JPG, PNG
- **対応**: BMP, WebP
- 透過情報は自動的にRGBに変換されます

### ファイル名
- 特に制限なし
- 日本語ファイル名も使用可能
- 重複しない名前を推奨

### 画像サイズ
- **最小推奨**: 384x384ピクセル以上
- **理想**: 512x512ピクセル以上
- 自動的にリサイズされるため、大きめの画像でもOK

### データ量

| データセット規模 | 各クラスの枚数 | 推奨エポック数 |
|----------------|--------------|--------------|
| 小規模 | 50-200枚 | 50-100 |
| 中規模 | 200-1000枚 | 100-150 |
| 大規模 | 1000枚以上 | 150-200 |

### 不均衡データセット

各クラスの枚数が大きく異なる場合でも大丈夫です:

```
dataset/
├── anime/          (2000枚) ← 多い
├── brush/          (500枚)
├── thick/          (300枚)
├── watercolor/     (800枚)
├── photo/          (150枚)  ← 少ない
├── 3dcg/           (400枚)
├── comic/          (600枚)
└── pixelart/       (250枚)
```

**自動調整機能:**
- クラス重み付けが自動計算されます
- 少数クラスの重要度が上がります
- トレーニング時に自動的に最適化されます

## 🚀 セットアップ手順

### 1. ディレクトリ作成

```bash
# 自動作成スクリプト実行
bash setup_dataset.sh
```

または手動で:

```bash
mkdir -p dataset/{anime,brush,thick,watercolor,photo,3dcg,comic,pixelart}
```

### 2. 画像配置

各クラスフォルダに画像ファイルをコピー:

```bash
# 例: animeフォルダに画像をコピー
cp /path/to/your/anime/images/*.jpg dataset/anime/
```

### 3. データセット確認

```bash
# 各クラスの画像数を確認
for dir in dataset/*/; do
    count=$(find "$dir" -type f \( -name "*.jpg" -o -name "*.png" \) | wc -l)
    echo "$(basename "$dir"): $count images"
done
```

### 4. トレーニング開始

```bash
python train.py --dataset dataset
```

## ✅ チェックリスト

トレーニング前に以下を確認:

- [ ] 8つのクラスフォルダすべてに画像が配置されている
- [ ] 各クラス最低でも50枚以上の画像がある
- [ ] 画像が正しく開ける (破損ファイルがない)
- [ ] ファイル形式が対応している (jpg, png, bmp, webp)
- [ ] GPUが正しく認識されている

```bash
# GPU確認
python -c "import torch; print(torch.cuda.is_available())"
```

## 🎯 データ収集のヒント

### 推奨データソース
- 自分で描いたイラスト
- パブリックドメインの画像
- クリエイティブ・コモンズライセンスの画像
- データセット配布サイト (Kaggle等)

### 注意事項
- ⚠️ 著作権に注意してください
- ⚠️ 商用利用の場合はライセンスを確認
- ⚠️ 個人情報が含まれないように

## 📊 データ品質のベストプラクティス

1. **多様性**: 同じアーティストの作品ばかりにならないように
2. **バランス**: 可能な限りクラス間の枚数を揃える
3. **品質**: 低解像度・ぼやけた画像は避ける
4. **一貫性**: クラス定義が曖昧な画像は除外
5. **検証**: 定期的にデータセットの内容を見直す

---

## 💡 よくある質問

**Q: クラスの枚数が大きく異なる場合はどうすればいい?**
A: そのままでOKです。自動的にクラス重み付けが行われます。

**Q: 画像サイズはどれくらいが良い?**
A: 最低384x384、理想は512x512以上。大きすぎても自動的にリサイズされます。

**Q: グレースケール画像は使える?**
A: はい。自動的にRGB 3チャンネルに変換されます。

**Q: データ拡張は自動で行われる?**
A: はい。トレーニング時に自動的に拡張(回転、反転、色調変更等)が適用されます。

**Q: 一部のクラスが少なすぎる場合は?**
A: 最低50枚は欲しいところですが、30枚程度でも試せます。ただし精度は下がります。
